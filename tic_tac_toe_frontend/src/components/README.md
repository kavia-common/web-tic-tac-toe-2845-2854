Components are currently implemented directly in app/page.tsx for simplicity and reduced bundle size.
If the app expands, move GameBoard, StatusPanel, and RestartButton into this directory as separate files.
